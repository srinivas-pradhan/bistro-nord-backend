bookATable:
  handler: src/bookATable/handler.BookATable
  role: !GetAtt WriteDynamoDBRole.Arn
  events:
    - httpApi:
        method: POST
        path: /bookATable
        authorizer:
          name: JwtAuthorizer
          scopes:
            - ServerAccessScope/write
